#!/bin/bash -e
#nodearray=$(jetpack config cyclecloud.node.template)
#hostname=$(hostname)
#placementgroup=$(jetpack config cyclecloud.node.placement_group none)

#echo $nodearray,$placementgroup,$hostname > /opt/cycle/jetpack/topology.csv 
